using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using WalletConnect;

public class WCQRCode : BindableMonoBehavior
{
    private WCSignClient _signClient;

    // Start is called before the first frame update
    void Start()
    {
        
    }
}
